{
  "name": "onlyfans-detector-api",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "playwright install chromium --with-deps",
    "heroku-postbuild": "playwright install chromium --with-deps"
  },
  "engines": {
    "node": "18.x"
  }
}
